import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Color.white.ignoresSafeArea()
            Text("Anyo")
                .font(.largeTitle)
        }
    }
}

#Preview {
    ContentView()
}
